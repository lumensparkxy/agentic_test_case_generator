Synthetic Meeting Room Booking app. Signed-in Employees Alice and Bob are separate users. Book room fields: Room (Atlas/Birch), Time slot (2026-10-01 10:00–11:00 Europe/Zurich), Attendees, Book. My bookings shows Booking ID, Room, Time slot, Attendees, Status, and Cancel booking where allowed; direct route /bookings/{bookingId}. Initial state: Alice owns Confirmed booking B-EXISTING for Atlas in that slot, 4 attendees; Birch is free. Reset fixtures before each test. Use an empty Atlas slot for valid-new-booking tests. Exact statuses: Confirmed, Cancelled. Conflict message: Room is already booked. Authentication implementation, pricing, reminders, recurring bookings, admin powers and integrations are unspecified; do not invent them. There is no live booking system or external target to execute against.

Explicit synthetic test-design assumption, accepted by the fixture owner: assume a controllable response-loss proxy in the test harness. For the interrupted-response retry case, it lets one valid booking submission reach the server and complete, records the returned booking ID as test evidence, then suppresses only that successful response to the browser. The browser can retry the same pending submission; the proxy forwards the retry normally so its returned booking ID and the single durable record after refresh can be compared with the recorded original. This is an assumed test setup for designing the case, not an implemented product feature or evidence that such a tool is available. It does not prescribe a product idempotency key, debouncing mechanism, new API route or status. Before real execution, provision and verify the proxy capability; no booking execution or response-loss experiment has been performed in this fixture.
